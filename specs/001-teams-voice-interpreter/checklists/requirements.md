# 规约质量校验清单：Teams 实时双向语音同传桥（macOS）

**用途**：在进入 `/speckit.clarify` 与 `/speckit.plan` 之前，验证规约（spec.md）的完备性与质量。
**创建日期**：2026-05-05
**关联规约**：[../spec.md](../spec.md)

---

## 内容质量

- [x] CHK001 不包含实现细节（编程语言、框架、具体 API SDK、代码结构等）：DeepSeek、BlackHole、Loopback、Python、Go、Web 仅在「假设」与 `[NEEDS CLARIFICATION]` 中作为用户**约束**出现，未渗透到功能需求与成功标准的描述里。
- [x] CHK002 聚焦于用户价值与业务诉求（双向同传的可懂、可信、可观测、可恢复）。
- [x] CHK003 对非技术利益相关者可读：所有功能需求与成功标准均以「用户能做什么 / 系统会发生什么」叙述，未引用源代码符号。
- [x] CHK004 所有强制章节均已填写：用户场景与测试、需求（功能需求 + 关键实体）、成功标准、假设。

## 需求完备性

- [x] CHK005 不存在 `[NEEDS CLARIFICATION]` 标记 —— **通过**：3 处原始标记已在 `/speckit.specify` 命令交互问答阶段全部解决：(1) FR-002 → BlackHole 2ch；(2) FR-007–010 → Whisper.cpp Streaming（本地）+ DeepSeek API（云）+ Edge-TTS（免费）；(3) FR-014 → CLI 子命令 + 本地 Web 控制台。
- [x] CHK006 需求可测试且无歧义：所有 FR 均使用「必须 / 不得 / 应当」配套数值阈值或可观测条件描述。
- [x] CHK007 成功标准可度量：SC-001 至 SC-010 均给出具体百分比、毫秒、秒、内存、CPU 占比阈值。
- [x] CHK008 成功标准与技术无关（不引用框架、语言、特定 API 实现细节）：SC 全部以用户感知（延迟、可懂度、稳定性、安装时间、错误恢复时间）表达；SC-009 反向约束「分发产物中不存在原生 App / Teams 插件」是用户可感知的产物形式而非实现细节。
- [x] CHK009 所有验收场景都已定义：4 个用户故事各自配 2–3 个验收场景，覆盖正常路径与关键边界。
- [x] CHK010 边界与异常已识别：覆盖 11 项（中英混讲、远端口音、专名/数字保留、Teams 系统音、蓝牙耳机、回声环路、配额耗尽、长会话、Teams 降噪、麦克风权限、虚拟驱动缺失）。
- [x] CHK011 范围边界清晰：v1 明确不含多人同时发言、说话人分离、其他语言对、外放扬声器场景的强保证、离线模式。
- [x] CHK012 依赖与假设已显式列出：「假设」段共 11 条，覆盖目标用户、硬件、会话规模、语言、延迟语义对齐、流式管线、运行表面、隐私、网络、回声、外部依赖。

## 功能就绪度

- [x] CHK013 每条功能需求都有清晰的验收准则：FR-001..FR-025 均可由验收场景或 SC 直接回测。
- [x] CHK014 用户故事覆盖主要业务流：上行（P1）、下行（P1）、状态可观测（P2）、启停与故障自愈（P3）四条独立切片构成完整闭环。
- [x] CHK015 功能与「成功标准」中可度量结果相互呼应：每条 SC 均能映射到至少一条 FR。
- [x] CHK016 实现细节未渗入规约：技术栈选择以 `[NEEDS CLARIFICATION]` 留待 `/speckit.clarify` 处理，不写入功能需求正文。

## 与项目宪章对齐

- [x] CHK017 性能预算（宪章原则 IV）已在 SC-001 / SC-002 / SC-003 / SC-004 / SC-010 中体现：首段译音 ≤ 1200 ms 硬 / ≤ 1000 ms 软（2026-05-07 宪章修订自 ≤ 800 ms，详见 .specify/memory/constitution.md 原则 IV 表）、整段端到端 p50 ≤ 2.5 s / p95 ≤ 4.0 s、24h 内存增长 ≤ 5%、稳态 RAM ≤ 500 MB、稳态 CPU ≤ 30%。
- [x] CHK018 UX 一致性（宪章原则 III）已在 FR-015 / FR-016 / FR-020 / SC-006 / SC-008 中体现：1 秒内反馈、随时可见延迟与最近识别/译文、错误两段式提示、设备切换不丢上下文。
- [x] CHK019 代码质量与测试纪律（宪章原则 I / II）由后续 plan / tasks 阶段承担，规约层无需重复约束。
- [x] CHK020 项目分发约束（FR-021 / SC-009）与「不开发原生 Mac App / Teams 插件」用户硬约束严格一致。

## 备注

- **全部 20 项校验通过**；规约已就绪可进入 `/speckit.clarify`（如希望进一步打磨）或直接 `/speckit.plan`（开始实施计划）阶段。
- 校验通过的项目在文档变更后应回归校验。
